---
interface Props {
  class?: string;
  style?: string;
}

const { class: className, style } = Astro.props;
---

<section class:list={["py-[3.75rem] xl:py-[5rem]", className]} style={style}>
  <slot />
</section>
