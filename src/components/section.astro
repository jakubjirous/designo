---
interface Props {
  class?: string;
  style?: string;
}

const { class: className, style } = Astro.props;
---

<section class:list={[className, "py-[3.75rem] xl:py-[5rem]"]} style={style}>
  <slot />
</section>
