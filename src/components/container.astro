---
interface Props {
  class?: string;
  style?: string;
}

const { class: className, style } = Astro.props;
---

<div class:list={["container relative mx-auto px-4 md:px-8 xl:px-16", className]} style={style}>
  <slot />
</div>
